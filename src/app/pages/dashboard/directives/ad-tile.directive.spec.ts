import { AdTileDirective } from './ad-tile.directive';

describe('AdTileDirective', () => {
  it('should create an instance', () => {
    const directive = new AdTileDirective();
    expect(directive).toBeTruthy();
  });
});
